{% extends 'base.html.twig' %}

{% block title %}Note{% endblock %}

{% block body %}
    <style>
        .content {
            display: flex;
            flex-direction: row;
            justify-items: center;
            align-items: center;
        }
        .content-show {
            background: #fffb91;
            color: #000;
            font-family: "Freestyle Script";
            width: 400px;
            height: 400px;
            margin: 0 auto;
            display: flex;
            flex-flow: row wrap;
            align-items: center;
            justify-content: center;
        }
        .content-show p {
            font-size: 42px;
            text-align: center;
        }
    </style>
    <div class="content">
        <div class="content-show">
            <p>{{ note.note }}</p>
            <p>{{ note.dueDate ? note.dueDate|date('d.m.Y H:i:s') : ''}}</p>
        </div>
    </div>

    <div class="nav">
        <a class="add-button" href="{{ path('note_index') }}">Retour à l'accueil</a>

        <a class="add-button" href="{{ path('note_edit', {'id': note.id}) }}">Modifier</a>
        {{ include('note/_delete_form.html.twig') }}
    </div>

{% endblock %}
